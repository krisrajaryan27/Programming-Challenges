package com.nirvana.learning.ongrid;

// function -> i/p; characterArray - {a, b, a, a, b}, int n => no of flips i.e a -> b or b -> a
// use atmost n flips, find the longest consecutive sequence of same characters

//n = 1 => o/p = 4

//length = 3
//{a, b, b, a, a}
public class LongestConsecutiveCharacters {
    public static void main(String[] args) {

    }

    // {a,b,b,a,b}

    // b b b -> 3 , 1

    // a == b

    //1. i != i-1 i== i+1

    //{a, a, a, a, b, b, b ,b,b,b,b,b}

    public static int longestConsecutiveSequence(char[] arr, int n) {

        for (int i = 0; i < arr.length; i++) {
            for (int j = 0; j < arr.length; j++) {

            }
        }

        return 0;
    }
}
